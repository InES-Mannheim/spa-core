/**
 * This packages contains the domain model of the core. The model itself is not
 * specific to any application. Thus, the model is define on a meta-level. The
 * application specific data models and schemes may be retrieved with the
 * provided services.
 */
package de.unima.core.domain;