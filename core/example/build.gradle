apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    flatDir {
        dirs '../lib', '../docs'
    }
}

dependencies {
   compile fileTree(dir: '../lib', includes: ['*.jar'], excludes: ["${coreName}-${coreVersion}.jar"])
   compile name: "${coreName}", version: "${coreVersion}" 
}
